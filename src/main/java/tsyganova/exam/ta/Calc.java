
package tsyganova.exam.ta;

import static java.lang.Math.pow;

public class Calc {
    
    public static double Calculate(double x)
    {
        return pow(x,3)+(3*pow(x,2))-(72*x)+90; //функція
    }
}
